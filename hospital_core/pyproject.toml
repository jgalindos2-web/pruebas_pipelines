[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hospital-core-demo"
version = "0.1.3"
description = "Paquete académico para gestión hospitalaria"
authors = [
    {name = "Jose Carlos Galindo", email = "jgalindos2@miumg.edu.gt"}
    ]
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
